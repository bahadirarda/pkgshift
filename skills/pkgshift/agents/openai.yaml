interface:
  display_name: "pkgshift"
  short_description: "Assess and shift package managers safely"
  default_prompt: "Use $pkgshift to assess readiness, preview, and safely execute a package manager or supported Bun-to-Deno runtime migration in this repository."
