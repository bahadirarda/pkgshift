interface:
  display_name: "pkgshift"
  short_description: "Shift package managers with exact approval"
  default_prompt: "Use $pkgshift to preview and safely execute a package manager migration in this repository."
